/**
 * Write a program that would print the area and
 * perimeter of a rectangle using the formula
 * P = 2 * length + 2 * width
 * A = length * width
*/

// #include <stdio.h>
// int main() { int x, y; printf("enter divident "); scanf(" %d", &x);printf("enter divisor "); scanf(" %d", &y); printf("%d  %d", x / y, x % y);}